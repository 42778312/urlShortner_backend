PORT=3000

# SQLite database file path (relative to project root, or absolute inside the container)
DATABASE_PATH=data/database.sqlite

# Base URL used to build the short URL returned to clients
BASE_URL=http://localhost:3000

# Length of generated short codes (6-8 recommended)
SHORT_CODE_LENGTH=6
